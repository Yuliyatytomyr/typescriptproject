import { makeStyles } from '@mui/styles';

export const useStyles = makeStyles({
    root: {
        boxShadow: '-1px -1px 20px 0px #88888885',
        borderRadius: '8px',
        padding: '15px',
        margin: '15px 0'
    }
})

